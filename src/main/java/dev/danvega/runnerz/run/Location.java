package dev.danvega.runnerz.run;

/**
 * Enumeration representing the location type where a run takes place.
 */
public enum Location {
	/** Run performed indoors (e.g., treadmill, indoor track). */
	INDOOR,
	/** Run performed outdoors (e.g., street, trail, park). */
	OUTDOOR
}
